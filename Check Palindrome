bool checkPalindrome(char str[]) {
    int i=0;
    int j= strlen(str)-1;
    while(i< j)
    {
        if(str[i]==str[j])
           {
            i++;
            j--;
           }
        else
           return false;
    }
    return true;
}
